server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/carlosramirezunad/microservicios-tutorial.git
          #skipSslValidation: true
          search-paths: config-data
          #username: carlos.taramuel95@gmail.com
          #password: Cataramuelr_18*
